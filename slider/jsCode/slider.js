function initSlider() {
    initImages()
    initArrows()
    initDots()
    moveDots()
    initLinks()
    moveLinks()
    initTitles()
}

document.addEventListener("DOMContentLoaded", function () {
    initSlider();
});